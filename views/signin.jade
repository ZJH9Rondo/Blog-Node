extends index

block content
  include components/flash
  div.sign_form
    form.ui.form.segment(method='post',enctype="multipart/form-data",action='/signin')
      div.field.required
        lable 用户名
        input(placeholder='用户名',type='text',name='name')
      div.field.required
        lable 密码
        input(placeholder='密码',type='password',name='password')
      input.sign_button(type='submit',value='登录')
